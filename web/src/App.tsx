/**
 * Finalidade: componente raiz da web (placeholder Sprint 0).
 * Como funciona: renderiza uma tela mínima; as telas da seção 7 entram a partir do Sprint 3.
 * Relações: montado por main.tsx.
 */
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 24 }}>
      <h1>Rota33</h1>
      <p title="Esqueleto inicial — telas a partir do Sprint 3">Setup concluído (Sprint 0).</p>
    </main>
  );
}
